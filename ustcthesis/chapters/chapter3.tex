% !TEX root = ../main.tex

\chapter{解耦式通用全身运动控制策略}

\section{上下肢解耦控制架构设计}

人形机器人在执行移动操作任务时，下肢负责维持稳定支撑和完成位姿调整，上肢负责执行抓取、挥拍、交互等精细动作。若将二者完全作为统一端到端控制问题求解，虽然理论上能够获得整体最优解，但在实际训练和部署中往往面临样本复杂度高、奖励设计困难、策略调试成本大以及迁移稳定性不足等问题。基于这一观察，本文提出上下肢解耦的通用全身控制架构，将移动稳定性与操作精度两个核心目标分别建模，并通过接口层实现统一协调。

在该架构中，下肢控制器接收速度命令、姿态目标和部分全身补偿信息，输出双腿关节动作以维持稳定 locomotion；上肢控制器接收来自遥操作模块或任务规划模块的末端目标、轨迹参考和姿态约束，并通过逆运动学和轨迹跟踪方法完成执行；躯干与状态管理模块则负责在上下肢之间同步关键姿态信息、监测质心偏移并触发必要的补偿策略。这样可以在保持系统模块化的同时，降低全身控制问题的求解难度。

从系统实现角度看，解耦架构还具有良好的可扩展性。对于纯遥操作场景，上肢接口可直接接收第 2 章重定向模块输出；对于自主任务场景，上肢接口也可无缝接收由视觉感知和高层规划器生成的末端目标。下肢控制器则可以在不同任务之间共享同一套基础移动能力，从而避免在每个任务中重复训练整套全身策略。解耦式通用全身运动控制架构如图~\ref{fig:decoupled_framework} 所示。

\begin{figure}[htbp]
    \centering
    \begin{tikzpicture}[
        % 定义全局样式
        font=\small\sffamily,
        >=Stealth,
        box/.style = {draw, rounded corners=3pt, minimum width=3.8cm, minimum height=1.2cm, align=center, thick},
        inputbox/.style = {box, fill=gray!10, draw=gray!80, dashed},
        upperbox/.style = {box, fill=cyan!5, draw=cyan!70!black},
        lowerbox/.style = {box, fill=orange!5, draw=orange!70!black},
        robotbox/.style = {box, fill=green!5, draw=green!70!black, minimum width=10cm},
        syncbox/.style = {box, fill=purple!5, draw=purple!70!black, minimum width=2.5cm, minimum height=1cm, dashed},
        arrow/.style = {->, thick, draw=black!80},
        feedback/.style = {->, thick, dashed, draw=blue!70!black}
    ]

    % --- 1. 高层输入层 ---
    \node[inputbox] (pico) {PICO VR \\ \footnotesize 输出: 目标位姿 $T_l, T_r$};
    \node[inputbox, right=2.5cm of pico] (nav) {用户指令 / 导航层\\ \footnotesize 输出: 目标速度 $v_x, v_y, \omega_z$};

    % --- 2. 解耦控制层 ---
    % 上肢部分
    \node[upperbox, below=1.5cm of pico] (ik) {\textbf{双臂逆运动学求解器}\\ \footnotesize (CasADi + IPOPT)};
    \node[upperbox, below=0.3cm of ik, minimum height=0.8cm] (kin) {Pinocchio 运动学模型\\ \footnotesize (Reduced Robot)};
    
    % 下肢部分
    \node[lowerbox, below=1.5cm of nav] (ppo) {\textbf{下肢强化学习 Locomotion}\\ \footnotesize (PPO 策略网络)};
    \node[lowerbox, below=0.3cm of ppo, minimum height=0.8cm] (state) {状态空间构造\\ \footnotesize (IMU, 关节历史, 接触)};

    % 中间同步层
    \node[syncbox] (sync) at ($(ik)!0.5!(ppo)$) {躯干与状态\\ 同步管理器};

    % --- 3. 机器人执行层 ---
    \node[robotbox, below=3.5cm of sync] (robot) {\textbf{机器人底层伺服控制器与物理本体}\\ \footnotesize (Unitree G1 / Taihu / MuJoCo)};

    % --- 连线 ---
    % 控制流 (实线)
    \draw[arrow] (pico) -- (ik) node[midway, left] {末端目标};
    \draw[arrow] (nav) -- (ppo) node[midway, right] {移动指令};
    \draw[arrow] (kin.south) |- ++(0,-0.6) -| ([xshift=-2.5cm]robot.north) node[pos=0.25, left] {$q_{arm}, \tau_{ff}$};
    \draw[arrow] (state.south) |- ++(0,-0.6) -| ([xshift=2.5cm]robot.north) node[pos=0.25, right] {$q_{leg}$};

    % 同步信息 (双向虚线)
    \draw[<->, dashed, thick, draw=purple!80!black] (ik.east) -- (sync.west) node[midway, above] {\footnotesize 姿态补偿};
    \draw[<->, dashed, thick, draw=purple!80!black] (ppo.west) -- (sync.east) node[midway, above] {\footnotesize 质心偏移};

    % 状态反馈流 (虚线)
    \draw[feedback] ([xshift=3.5cm]robot.north) |- ++(0,1.2) -| (state.south) node[pos=0.25, right] {IMU 与下肢反馈};
    \draw[feedback] ([xshift=-3.5cm]robot.north) |- ++(0,0.8) -| (kin.south) node[pos=0.25, left] {当前双臂关节状态};

    % 背景虚线框包裹解耦层 (可选，用于划分区域)
    \node[draw=black!30, dashed, inner sep=0.4cm, fit=(ik) (kin) (ppo) (state) (sync), label={[text=black!60]above left:解耦式通用全身运动控制层}] {};

    \end{tikzpicture}
    \caption{解耦式通用全身运动控制架构图}
    \label{fig:decoupled_framework}
\end{figure}


\section{上肢遥操作}

\subsection{系统组成与数据流}

在本文提出的上下肢解耦控制框架中，上肢模块主要承担精细操作与末端轨迹跟踪任务。与下肢侧强调稳定移动不同，
上肢控制更关注末端位姿的准确表达、关节解的连续性以及对不同机器人平台的适配能力。
为此，本文基于 PICO 设备构建了上肢遥操作系统，并将其与双臂逆运动学求解器及机器人控制器连接起来，形成从操作者动作输入到机器人双臂执行的完整链路。

该系统整体可划分为三个部分。第一部分为遥操作输入模块，负责通过 XR 设备实时获取操作者双臂运动状态，并生成左右臂目标位姿；第二部分为逆运动学模块，
负责结合目标末端位姿与机器人当前关节状态求解双臂关节角；第三部分为机器人执行模块，负责将求解结果发送至上肢控制器，实现双臂轨迹跟踪。
与此同时，机器人头部图像通过图像链路回传到遥操作端，为操作者提供第一视角反馈，从而构成闭环遥操作界面。

从主程序实现流程来看，系统在每个控制周期内首先读取 XR 设备输出的左右臂目标位姿，再读取机器人当前双臂关节位置和速度作为逆解初值与状态反馈；随后调用双臂逆运动学模块求解当前时刻的目标关节角及前馈力矩；最后由上肢控制器将这些结果下发给机器人。通过这一流程，本文实现了“人体动作输入--机器人状态反馈--逆解求解--控制执行”的闭环上肢遥操作链路。

\subsection{基于 PICO 的双臂目标位姿获取}

本文采用 PICO 作为上肢遥操作输入设备。系统通过 TeleVuer 接口读取操作者双手和双臂的运动状态，并将其转换为适用于机器人控制的左右臂目标位姿表示。与直接映射人体关节角的方案不同，本文采用基于末端位姿的遥操作方式，即将操作者左右腕部在空间中的位置与姿态统一表示为齐次变换矩阵，并将其作为双臂逆运动学模块的输入。

这种表示方式具有两方面优势。首先，末端位姿描述与具体机器人关节结构解耦，因而更适合不同机器人平台之间的迁移；其次，末端位姿能够更直接反映抓取、递送、挥拍等操作任务中的空间目标，有利于在遥操作模式与自主规划模式之间共享同一套上肢控制接口。因此，在本文的系统设计中，无论目标位姿来自 PICO 遥操作输入还是来自后续任务规划模块，均可统一交由同一套双臂逆运动学模块处理。

此外，遥操作系统还支持图像回传与数据记录功能。机器人头部与腕部相机图像可以回传至 XR 设备，为操作者提供视觉反馈；同时，系统可同步记录图像、左右臂状态和控制输出，用于后续数据集构建、离线分析和算法调试。这为上肢遥操作从单纯控制接口扩展到数据采集平台提供了基础。

\subsection{双臂逆运动学求解方法}

在获得左右臂目标位姿后，系统需要将末端空间目标转换为机器人双臂关节空间指令。为此，本文基于 Pinocchio 构建机器人上肢运动学模型，并采用 reduced robot 的方式锁定下肢、腰部以及部分手部关节，仅保留双臂主要活动自由度参与逆运动学求解。通过这种方式，可以在保证双臂可达性的前提下显著降低求解规模，提高实时性与稳定性。

在模型构建完成后，本文分别在左右腕部附近定义左右末端参考帧 $L_{ee}$ 与 $R_{ee}$，用作双臂末端的操作点。对于给定的左右臂目标位姿，系统构建包含位置误差、姿态误差、正则项和平滑项的优化目标函数：
\begin{equation}
J = w_p \lVert e_p \rVert^2 + w_r \lVert e_r \rVert^2 + w_q \lVert q \rVert^2 + w_s \lVert q - q_{t-1} \rVert^2,
\end{equation}
其中，$e_p$ 表示左右末端的位置误差，$e_r$ 表示左右末端的姿态误差，$q$ 为当前时刻待求的双臂关节角，$q_{t-1}$ 为上一时刻关节解。位置项与姿态项用于提升末端跟踪精度，正则项用于抑制过大的关节偏移，平滑项则用于保证相邻时刻求解结果的连续性，从而减小遥操作过程中的抖动现象。算法~\ref{alg:dual-arm-ik} 给出了双臂逆运动学的核心求解流程。

\begin{algorithm}[ht]
  \caption{双臂逆运动学求解流程}
  \label{alg:dual-arm-ik}
  \SetAlgoLined
  \KwIn{左臂目标位姿 $T_l$，右臂目标位姿 $T_r$，当前关节状态 $q_{t-1}$}
  \KwOut{双臂目标关节角 \texttt{sol\_q}，前馈力矩 \texttt{sol\_tauff}}

  读取机器人上肢 reduced robot 模型及关节上下界\;
  在左右腕部定义末端参考帧 $L_{ee}$ 和 $R_{ee}$\;
  将 $T_l$、$T_r$ 设置为当前时刻逆运动学目标\;
  根据当前关节变量 $q$ 计算左右末端位置误差 $e_p$\;
  根据当前关节变量 $q$ 计算左右末端姿态误差 $e_r$\;
  构建目标函数
  $
    J = 50 \lVert e_p \rVert^2 + \lVert e_r \rVert^2
    + 0.02 \lVert q \rVert^2 + 0.1 \lVert q - q_{t-1} \rVert^2
  $\;
  在关节限位约束下调用 IPOPT 求解最优关节角 \texttt{sol\_q}\;
  基于求得的关节角和机器人动力学模型计算前馈力矩 \texttt{sol\_tauff}\;
  输出 \texttt{sol\_q} 和 \texttt{sol\_tauff}\;
\end{algorithm}

在求解器实现上，本文采用 CasADi 建立符号优化问题，并调用 IPOPT 进行数值求解。为保证求解结果满足机器人硬件约束，优化过程中显式加入关节上下界约束。最终，逆运动学模块输出双臂目标关节角 \texttt{sol\_q}，并结合机器人动力学模型进一步计算逆动力学前馈力矩 \texttt{sol\_tauff}。这一设计使得上肢控制器不仅能够根据目标末端位姿产生关节位置指令，还能够通过前馈补偿提高跟踪执行的稳定性。

\subsection{Taihu 平台迁移与仿真验证}

在 Unitree G1 平台实现双臂遥操作与逆运动学求解后，本文进一步将同一套方法迁移到 Taihu 全尺寸机器人平台。迁移过程中，逆运动学的整体建模思路保持不变，核心修改集中在机器人 URDF 模型、关节命名方式、锁定关节集合以及末端参考帧的重新定义上。也就是说，本文并未为 Taihu 单独设计一套新的上肢求解框架，而是在保留“末端位姿输入 + 约束优化求解”这一核心思路的基础上，完成了面向新平台的工程适配。

具体而言，Taihu 平台采用与 G1 不同的关节命名和机械结构，因此系统需要根据 Taihu 机器人上肢的实际关节布局重新定义 reduced robot，并在左右腕部关节附近构造新的末端参考帧。同时，由于全尺寸机器人在臂长、可达空间和关节限位上与 G1 存在差异，原有上肢末端目标在新平台上未必能够直接复用，因此仍需结合具体平台参数对目标位姿和求解约束进行适配。

为降低直接真机调试的风险，本文在 Taihu 平台迁移过程中进一步结合 MuJoCo 与可视化环境对双臂逆解模块进行了验证。通过在仿真环境中输入左右臂目标位姿并观察机器人末端跟踪效果，可以在不上真机的情况下检查求解器在新平台上的可达性、连续性和稳定性。实验表明，本文所采用的上肢遥操作与逆运动学框架具有较好的跨平台复用能力，可为后续全尺寸人形机器人的双臂操作研究提供基础。

\section{下肢强化学习 Locomotion 策略}

\subsection{策略训练环境配置}

下肢控制策略在 Isaac Gym 或 Isaac Lab 等大规模并行仿真环境中进行训练。仿真环境负责提供机器人动力学求解、接触模拟、地形变化以及外部扰动注入等功能，以支撑高频率的并行策略优化。训练过程中，机器人从不同初始姿态、速度指令和环境参数出发，不断学习如何维持平衡并实现期望移动行为。

为了提升策略在真实系统中的适应能力，本文在训练环境中引入域随机化机制，对摩擦系数、机身质量分布、执行器参数、传感噪声和地形高度扰动等因素进行随机采样。这样可以使策略在训练阶段接触更广泛的动力学条件，降低部署到真实机器人时因建模误差带来的性能退化风险。对于复杂地形或外部碰撞情形，也可通过额外扰动课程逐步增加训练难度。

\subsection{状态空间与奖励设计}

状态空间设计直接影响强化学习策略能否充分利用机器人本体信息并形成稳定步态。本文在下肢策略中使用的观测包括 IMU 姿态、机身角速度、各下肢关节角与关节速度、历史速度命令、足端接触信息以及必要的短时历史状态。通过这些输入，策略能够同时感知机器人当前姿态、运动趋势和外部支撑状态，从而做出合理动作决策。

动作空间采用下肢关节位置目标或位置增量的形式表示，覆盖左右腿共 12 个关键关节。相较于直接输出力矩控制指令，位置目标形式更便于与现有伺服执行接口兼容，也有助于提升训练稳定性。底层执行器再结合 PD 控制器或平台已有的关节控制模块完成实际跟踪。

奖励函数由多个子项组成，包括速度跟踪奖励、姿态稳定奖励、足端接触一致性奖励以及能量消耗惩罚等。其中，速度跟踪项鼓励策略准确跟随前进、后退、旋转和横向移动指令；平衡稳定项用于抑制过大的机身姿态偏差和跌倒；能量效率项则用于减少无效摆动和过大关节动作，以提升策略在真机上的可执行性。通过对不同奖励项进行加权组合，策略能够在稳定性与机动性之间取得平衡。

\subsection{网络结构与训练过程}

本文下肢策略采用基于 PPO 的强化学习算法进行训练。策略网络和价值网络均使用多层感知机结构，通过若干隐藏层对输入状态进行非线性特征提取，并输出各关节动作分布参数。PPO 算法在策略更新时具有较好的稳定性和实现成熟度，因此适合作为基础 locomotion 技能训练的核心优化方法。

在训练流程中，系统首先从较简单的速度命令和稳定站立任务开始，待策略具备基本平衡能力后，再逐步增加方向切换、复合速度命令和外部扰动。通过课程式训练，策略能够逐渐适应更复杂的控制目标。对于学习率、批次大小、折扣因子和裁剪系数等超参数，本文暂在附录中以占位方式保留，后续将结合最终实验配置补全。

\subsection{策略性能评估}

为了评价下肢 locomotion 策略的实用性，本文从速度覆盖范围、任务成功率和鲁棒性测试三方面进行分析。速度覆盖范围反映策略在不同方向上的可控运动区间；任务成功率用于统计在规定时间或路径约束下完成动作的比例；鲁棒性测试则关注策略在地形变化、外力扰动和指令切换条件下的稳定性。

结合当前提纲，本文重点关注前进、后退、原地旋转和左右平移四类基础运动模式。由于真机和仿真统计结果尚待整理，本节暂保留量化指标占位，如最大前进速度为 X\,m/s、旋转角速度为 X\,rad/s、任务成功率为 XX\% 等。后续补充正式实验结果后，可进一步将这些指标整理为统一表格，并与不同训练设置下的策略表现进行对比。

\section{Sim-to-Real 迁移与真机验证}

仿真训练获得的控制策略能否平稳部署到真实机器人，是评价系统工程价值的重要标准。为减小仿真与真实系统之间的差异，本文在控制部署阶段结合动力学参数辨识、执行器响应适配、传感器标定和控制频率对齐等手段，对策略输入输出接口进行统一修正。同时，通过在训练阶段引入域随机化，使策略提前适应一定范围内的模型偏差和环境变化。

在真机验证方面，本文计划围绕四类基础 locomotion 模式展开实验，包括前进、后退、原地旋转和左右平移，并考察不同速度指令下的稳定性、跟踪误差和能耗情况。对于上肢部分，则重点验证遥操作轨迹跟踪精度以及与下肢运动同时执行时的协调表现。对于尚未完成的统计结果，本文保留速度跟踪误差、关节力矩分布和单位任务能耗等指标占位，以待后续实验补齐。

总体而言，解耦式控制策略将遥操作、强化学习和任务接口三者连接起来，使本文的方法既能支撑基础移动技能，又能支持上层感知与规划模块的接入。这为第 4 章的任务级闭环验证奠定了基础。
